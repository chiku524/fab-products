# Harbor Suite — engine test matrix

Record results when you validate a **packaged** plugin build (not only the monorepo checkout).

| UE minor line tested | Windows editor | Package script label | Result | Notes |
|----------------------|----------------|----------------------|--------|-------|
| 5.7.x | Win64 | `5.7` | Pass (editor compile) | `Build.bat HarborEditor Win64 Development` on repo checkout |
| | | | | |

Update this table before each Fab upload.
