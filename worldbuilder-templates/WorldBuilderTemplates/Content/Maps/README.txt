World Builder Templates — map folder
======================================

Shipped samples (regenerate with scripts/create-sample-maps.ps1 if needed):

  WBT_Template_Outdoor.umap
  WBT_Template_IndoorBlockout.umap
  WBT_Template_Ocean.umap
  WBT_Template_City.umap

Add more .umap files here when authoring; they are mounted under
/WorldBuilderTemplates/Maps/ and listed in **Window → Level Editor → World Builder Templates**.

Use **Refresh list** in that panel after adding assets (or restart the editor).
